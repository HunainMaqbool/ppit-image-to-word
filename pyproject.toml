[project]
name = "ppit"
version = "0.1.0"
description = "Basic Image-to-Word Converter (MVP) - OCR text extraction with formatting preservation"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytesseract",
    "pillow",
    "python-docx",
    "streamlit",
    "numpy",
]
